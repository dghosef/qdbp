(* FIXME: Change the parameter names? *)
defer := {val after|
  result := after!.
  _ := val!.
  result
}

defer! {"finished doing fancy math" Print.}
  after: {
    _ := "doing fancy math" Print.
    1 + 1. + 3. * 15.
  }.